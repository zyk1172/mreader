name: Manga Vision Debug Reader

on:
  push:
    branches:
      - feature/manga-vision-layer
    paths:
      - scripts/apply_manga_vision_debug_reader.py
      - .github/workflows/manga-vision-debug-reader.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feature/manga-vision-layer
          fetch-depth: 0
      - name: Apply debug overlay integration
        run: python3 scripts/apply_manga_vision_debug_reader.py
      - name: Commit integrated sources
        run: |
          rm scripts/apply_manga_vision_debug_reader.py
          rm .github/workflows/manga-vision-debug-reader.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "feat: expose Manga Vision debug overlay in reader"
          git push origin feature/manga-vision-layer
