name: Phase 4 offline region persistence

on:
  push:
    branches:
      - codex/translation-comic-integration
    paths:
      - .github/workflows/phase4-offline-region-patch.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: codex/translation-comic-integration
          fetch-depth: 0
      - name: Apply persistence fix
        run: python3 .github/phase4_offline_region_patch.py
      - name: Commit and clean helpers
        shell: bash
        run: |
          rm -f .github/phase4_offline_region_patch.py .github/workflows/phase4-offline-region-patch.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'fix: persist translation layout safe regions offline'
          git push origin HEAD:codex/translation-comic-integration
