# Visual OCR provider debug handoff

If real-device visual OCR still returns no usable text after this app-side fix, collect the configured provider/model identifier and the exact returned API error or assistant content. Do not include API keys.

This distinguishes provider/model image-input incompatibility from app-side response-contract failures.
